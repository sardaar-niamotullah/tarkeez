import 'package:flutter/widgets.dart';

class SendConnectionButton extends StatelessWidget {
  const SendConnectionButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}